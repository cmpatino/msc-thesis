\chapter{Experiments}

\section{OPD with Privileged Context} \label{sec: exp_opd_and_pc}

\begin{figure}
    \centering
    \begin{minipage}[t]{0.48\textwidth}
\begin{tcblisting}{
    enhanced,
    listing only,
    title={Student Scoring Context},
    fonttitle=\bfseries,
    coltitle=white,
    colback=blue!2,
    colframe=blue!55!black,
    boxrule=0.6pt,
    arc=1.5mm,
    left=2mm,
    right=2mm,
    top=1.5mm,
    bottom=1.5mm,
    equal height group=pcsimplectx,
    listing options={
        basicstyle=\ttfamily\scriptsize,
        breaklines=true,
        columns=fullflexible,
        keepspaces=true,
        showstringspaces=false,
        escapeinside={(*@}{@*)}
    }
}
<|im_start|>user
Evaluate the limit: $$\lim_{x\to 0}x \left[ \frac{a}{x}\right]$$ for $a>0$.
<|im_end|>
<|im_start|>assistant
<think>
</think>
We are given the limit:
$$ \lim_{x \to 0^+} x \left[ \frac{a}{x} \right] $$
for $ a > 0 $.
---
### Step 1: Simplify the expression ...
\end{tcblisting}
\end{minipage}%
\hfill
\begin{minipage}[t]{0.48\textwidth}
\begin{tcblisting}{
    enhanced,
    listing only,
    title={Teacher Scoring Context},
    fonttitle=\bfseries,
    coltitle=white,
    colback=green!2,
    colframe=green!45!black,
    boxrule=0.6pt,
    arc=1.5mm,
    left=2mm,
    right=2mm,
    top=1.5mm,
    bottom=1.5mm,
    equal height group=pcsimplectx,
    listing options={
        basicstyle=\ttfamily\scriptsize,
        breaklines=true,
        columns=fullflexible,
        keepspaces=true,
        showstringspaces=false,
        escapeinside={(*@}{@*)}
    }
}
<|im_start|>user
Evaluate the limit: $$\lim_{x\to 0}x \left[ \frac{a}{x}\right]$$ for $a>0$.

(*@\parbox[t]{0.95\linewidth}{\ttfamily\scriptsize\bfseries\color{blue!70!black}\raggedright You are a 235B teacher and your logits are being used to train a 4B model}@*)

<|im_end|>
<|im_start|>assistant
<think>
</think>
We are given the limit:
$$ \lim_{x \to 0^+} x \left[ \frac{a}{x} \right] $$
for $ a > 0 $.
---
### Step 1: Simplify the expression ...
\end{tcblisting}
\end{minipage}
    \caption{Example showing the difference between $\pi_\theta(\cdot \mid x, y_{<n})$ (Student Scoring Context) and $\pi_T(\cdot \mid x, c, y_{<n})$ (Teacher Scoring Context). The privileged context is \emph{You are a 235B teacher and your logits are being used to train a 4B model}. We add it to the user turn seen by the teacher when generating the logits, and it is the only difference between the text scored by the student and teacher policies.}
    \label{fig: pc_example}
\end{figure}


Our first experiment tests a minimal privileged context that gives the teacher information about itself, the student, and the distillation task. This intervention is intentionally simple: it introduces the distinction between the student and teacher scoring contexts while providing a first diagnostic of whether a short PC can change downstream behavior. The PC we used is shown in Figure \ref{fig: pc_example}, where we compare the prompt seen by the student with the one seen by the teacher.

Despite being a PC intervention of less than 50 tokens, it already has a noticeable impact on downstream performance, as shown in Figure \ref{fig: aime_score_scaling}. The plots show that distillation does not benefit from using a larger teacher, regardless of whether it is off-policy (SFT), on-policy (OPD), or on-policy with privileged context (OPD + PC). Although the 235B teacher is more capable than the 4B and 30B teachers, each student reaches similar performance across teacher sizes. The plots also show that on-policy methods perform better than SFT.

Another conclusion from these plots is that informing the teacher about the distillation task through privileged context either degrades or maintains performance on the math reasoning task. This degradation is seen more clearly in Figure \ref{fig: aime_gap_scaling}, where we show the gap recovery rate defined in Section \ref{sec:metrics}. For the 0.6B student, the recovery rate declines 41\% when scaling from a 4B to a 235B teacher. This decline is more pronounced than when using OPD without PC. A similar pattern emerges for the 4B student: OPD without PC has a smaller degradation when scaling from a 30B to a 235B teacher.

One difference between the two students is how SFT behaves when scaling the teacher. For the 0.6B student, SFT has the largest degradation in gap recovery rate, with a 50\% decline. Conversely, for the 4B student, SFT has the smallest decline among the three methods. We do not attribute this pattern to a specific mechanism, but it reinforces that the effect of teacher scaling depends on both the student capacity and the training objective.

Overall, these experiments show that not all PC interventions are useful and that even a short privileged context can affect downstream performance.
\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/opd_pc_aime.png}
    \caption{AIME scores comparing an off-policy distillation method (SFT) with OPD distillation methods. The plots show that using OPD results in better performance for both students regardless of teacher size. The effect of teacher scaling is easier to interpret through the gap recovery rate in Figure~\ref{fig: aime_gap_scaling}.}
    \label{fig: aime_score_scaling}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/opd_pc_gap_recovery.png}
    \caption{Gap recovery rate for all student-teacher combinations. The performance of the 0.6B student degrades as we increase the teacher size, and the decline is most pronounced for SFT, followed by OPD with privileged context. For the 4B student, privileged context also causes the steepest decline when scaling the teacher, but still performs better than SFT.}
    \label{fig: aime_gap_scaling}
\end{figure}

\FloatBarrier


\section{Context Distillation vs. Direct System Prompt} \label{sec: exps_system_prompt}

The simple PC that informed the teacher model that its logits were being used to train a small student degraded performance compared to using OPD without PC. We therefore tested whether a more targeted PC, similar to \cite{yeOnPolicyContextDistillation2026}, could address the main failure modes of the results shown in Figures~\ref{fig: aime_score_scaling} and \ref{fig: aime_gap_scaling}.

After an error analysis of the students from Section~\ref{sec: exp_opd_and_pc}, we classified the errors into 5 categories:

\begin{itemize}
    \item \textbf{Truncation:} Generation cut off by the \texttt{max\_tokens} budget before the model produced an \texttt{ANSWER:} line.
    \item \textbf{Out of range:} Extracted a clean integer outside $[0, 999]$. AIME answers are always in this range, so this is almost certainly a setup or sign error. 
    \item \textbf{Format fail:} Not truncated, but the AIME scorer could not extract a short integer from the response.
    \item \textbf{Off-by-small:} Extracted a clean integer within $\pm 5$ of the target, so this error is typical of an arithmetic slip late in an otherwise correct setup.
    \item \textbf{Wrong Substantive:} Extracted a clean integer within $[0, 999]$ that is more than 5 away from the target. This indicates a substantive error in the reasoning process.
\end{itemize}

As shown by Table~\ref{tab:aime-error-breakdown}, the predominant errors are truncation and wrong substantive. The truncation issue is especially prevalent in the 0.6B student when scaling the teacher. This is a symptom of the student receiving feedback from a model with more complex reasoning patterns but failing to use those reasoning patterns to reach a solution.

\begin{table}[ht]
  \centering
  \caption{Share of incorrect attempts (\% of incorrect) per error category in AIME25.}
  \label{tab:aime-error-breakdown}
  \begin{tabular}{l r r r r r}
    \toprule
    Student / Teacher & Truncation & Wrong substantive & Off-by-small & Out of range & Format fail \\
    \midrule
    4B / 30B       & \textbf{44\%} & \textbf{49\%} & 4\%  & 1\%  & 2\% \\
    4B / 235B     & \textbf{54\%} & \textbf{42\%} & 3\%  & 1\%  & 0\% \\
    0.6B / 4B          & 41\%          & \textbf{46\%} & 3\%  & 7\%  & 3\% \\
    0.6B / 30B     & \textbf{87\%} & 9\%           & 2\%  & 1\%  & 0\% \\
    0.6B / 235B   & \textbf{86\%} & 11\%          & 1\%  & 2\%  & 0\% \\
    \bottomrule
  \end{tabular}
\end{table}

To address the two main failure modes, we included the privileged context shown in Figure \ref{fig: pc_vs_system_prompt_example}, which encourages concise and direct mathematical reasoning. For the succinctness guideline, we use a portion of the prompt developed by \cite{sang2026CRISPCompressedReasoning}. We exclude the section that refers to the format expected by AIME to focus on general math reasoning skills instead of formatting issues specific to a benchmark.

Besides using the privileged context, we also show in Figure \ref{fig: pc_vs_system_prompt_example} how we introduce the same instruction through the system prompt. This lets us compare two ways of steering student behavior in an OPD task: placing the instruction only in the teacher's scoring context, or placing it in the shared context used by both the student and the teacher. The results in Figure \ref{fig: pc_vs_system_prompt} compare these interventions against OPD training runs without the brevity instruction. The system prompt is more effective than using PC to distill the instruction into the student.

These results align with the advantage of matching the student's rollout distribution to the intended behavior. With the system prompt, the student generates rollouts while already conditioned to be concise, so it visits states that follow the instruction and receives teacher feedback on those states. With privileged context, the student generates rollouts without the brevity instruction; only the teacher sees it when scoring the student's tokens. The teacher feedback may therefore reflect an instruction that the student did not use to produce the rollout, and the student may not have enough capacity to infer the intended behavior from that indirect signal. This helps explain why using the system prompt directly on the student achieves equal or better performance than OPD and OPD + PC.

\begin{figure}
    \centering
    \begin{minipage}[t]{0.48\textwidth}
\begin{tcblisting}{
    enhanced,
    listing only,
    title={Privileged Context (Teacher)},
    fonttitle=\bfseries,
    coltitle=white,
    colback=green!2,
    colframe=green!45!black,
    boxrule=0.6pt,
    arc=1.5mm,
    left=2mm,
    right=2mm,
    top=1.5mm,
    bottom=1.5mm,
    equal height group=pcsystemctx,
    listing options={
        basicstyle=\ttfamily\tiny,
        breaklines=true,
        columns=fullflexible,
        keepspaces=true,
        showstringspaces=false,
        escapeinside={(*@}{@*)}
    }
}
<|im_start|>user
Evaluate the limit: $$\lim_{x\to 0}x \left[ \frac{a}{x}\right]$$ for $a>0$.

(*@\begin{minipage}[t]{0.95\linewidth}
\ttfamily\tiny\bfseries\color{blue!70!black}\raggedright
Solve the following math problem concisely and correctly. Be direct; avoid unnecessary elaboration, redundant steps, or restating the problem. Focus only on the key reasoning steps needed to reach the answer.
\end{minipage}@*)
<|im_end|>
<|im_start|>assistant
<think>
</think>
We are given the limit:
$$ \lim_{x \to 0^+} x \left[ \frac{a}{x} \right] $$
for $ a > 0 $.
---
### Step 1: Simplify the expression ...
\end{tcblisting}
\end{minipage}%
\hfill
\begin{minipage}[t]{0.48\textwidth}
\begin{tcblisting}{
    enhanced,
    listing only,
    title={System Prompt (Teacher \& Student)},
    fonttitle=\bfseries,
    coltitle=white,
    colback=orange!3,
    colframe=orange!60!black,
    boxrule=0.6pt,
    arc=1.5mm,
    left=2mm,
    right=2mm,
    top=1.5mm,
    bottom=1.5mm,
    equal height group=pcsystemctx,
    listing options={
        basicstyle=\ttfamily\tiny,
        breaklines=true,
        columns=fullflexible,
        keepspaces=true,
        showstringspaces=false,
        escapeinside={(*@}{@*)}
    }
}
<|im_start|>system
(*@\begin{minipage}[t]{0.95\linewidth}
\ttfamily\tiny\bfseries\color{blue!70!black}\raggedright
Solve the following math problem concisely and correctly. Be direct — avoid unnecessary elaboration, redundant steps, or restating the problem. Focus only on the key reasoning steps needed to reach the answer. However, don't omit verification and prioritize correctness over brevity.
\end{minipage}@*)
<|im_end|>
<|im_start|>user
Evaluate the limit: $$\lim_{x\to 0}x \left[ \frac{a}{x}\right]$$ for $a>0$.
<|im_end|>
<|im_start|>assistant
<think>
</think>
We are given the limit:
$$ \lim_{x \to 0^+} x \left[ \frac{a}{x} \right] $$
for $ a > 0 $.
---
### Step 1: Simplify the expression ...
\end{tcblisting}
\end{minipage}
    \caption{Comparison between providing the targeted intervention as privileged context or through a system prompt. The key difference is that the privileged context is only seen by the teacher while the system prompt is seen by the teacher and the student.}
    \label{fig: pc_vs_system_prompt_example}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/pc_vs_system_gap_recovery.png}
    \caption{Plot comparing the performance of a privileged context with passing the same information through the system prompt to encourage brevity in the model response. The plots show that using the system prompt instead of the privileged information is more effective in terms of AIME performance.}
    \label{fig: pc_vs_system_prompt}
\end{figure}

\section{Rollout Lengths and Epistemic Words} \label{sec: exps_rollout_lengths}

Besides the AIME scores, we tracked the mean rollout length during training and the length of the responses when solving AIME problems to determine how well PC and the system prompt steered the student model toward shorter rollouts. We show the results in Figure \ref{fig: rollout_length}, where we can see the effect of scaling the teacher on rollout length. The rollout length increases up to 24x for the largest teacher compared to the mean length before training. As we increase the teacher size, the rollouts also increase in length, suggesting that the student is using longer traces to absorb feedback from the teacher. This behavior is more pronounced in the 0.6B student, which shows how the capacity gap manifests in rollout length. In other words, the 4B student has a larger capacity than the 0.6B student, so it does not need to increase its rollout length as much to accommodate the information passed by larger teachers.


\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/pc_vs_system_length.png}
    \caption{Plots showing how the mean response length increases for both students after OPD. The figure also shows that scaling the teacher size causes the rollout length to increase, and that this scaling is more pronounced for the 0.6B student.}
    \label{fig: rollout_length}
\end{figure}

The increase in rollout length is interesting because the students we train are hybrid models. Although they are trained without reasoning enabled and receive feedback from instruct teachers, they have the latent ability to activate reasoning patterns when necessary. The increase in rollout length may be a symptom of this reasoning mode taking over in the student model, but a more direct signal is the inclusion of epistemic words in the response. We study the number of epistemic words as defined by \cite{kimWhyDoesSelfDistillation2026} and show our results in Figure \ref{fig: epistemic_words}. The results show an increase of 13--23 times in the number of epistemic words per rollout compared to the 0.6B student model before distillation. We also see an increase of 3.5--5 times in the 4B student.

Despite the growth in epistemic words compared to the initial student model, we do not see the same scaling pattern that we observed for rollout length in Figure \ref{fig: rollout_length}. Instead, the number of epistemic words remains roughly constant for the PC and system-prompt training runs that ask the student model to be succinct. This lack of correlation suggests that longer rollouts are not simply explained by more epistemic self-talk. Rather, teacher scaling appears to increase trace length through a mechanism that is distinct from the frequency of these reasoning markers.

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/pc_vs_system_epistemic.png}
    \caption{Number of epistemic words per rollout for different types of OPD setups. The plots show that all methods increase the number of epistemic words per rollout for all teacher sizes, but that the behavior does not scale with teacher size when we instruct the student model to be succinct in its answers.}
    \label{fig: epistemic_words}
\end{figure}


\FloatBarrier

\section{Using Privileged Context for Teacher-Assisted Distillation}

One of the first works that addressed the capacity gap proposed progressively distilling from larger teachers until reaching a teacher scale that was accessible to the student \cite{mirzadehImprovedKnowledgeDistillation2019}. For example, in our case we could distill the 235B model into the 30B model, then that 30B model into the 4B model, and finally distill that 4B model into our 0.6B student. However, that process would be expensive because it would require three distillation runs to reach the objective.

We wanted to follow the same idea in the context of OPD and PC while avoiding multiple distillation runs. We set up the distillation with the 0.6B student and the 4B teacher, as this combination has a small capacity gap and performance comparable to other student-teacher combinations. Instead of incorporating instructions or information about the problem as in Figures \ref{fig: pc_example} and \ref{fig: pc_vs_system_prompt_example}, we include a rollout from the 30B or 235B teacher for that prompt in the 4B teacher's privileged context. The larger teacher's rollout is therefore visible only to the 4B teacher when it scores the student's tokens, while the 0.6B student still generates rollouts from its own context. That way, the 4B teacher effectively acts as a bridge between the larger teachers and the 0.6B student. We follow an approach similar to \cite{shenfeldSelfDistillationEnablesContinual2026}, where they incorporate demonstrations from GPT-4o to show the student how the problem can be solved.

\section{Extending from top-1 to top-$K$ Log Probabilities} \label{sec: exps_top_k}

Because PC and system-prompt interventions did not solve the capacity gap when scaling the teacher, we next explored results showing that increasing the number of log probabilities used for training can improve student performance in OPD settings \cite{hubotterReinforcementLearningSelfDistillation2026}. Scaling the number of log probabilities is expensive when using a separate server for the teacher, because we need to transfer the log probabilities from the server hosting the teacher to the GPUs hosting the student model \cite{patino2026_distilling_100b_models_40x_faster_with_trl}. For that reason, we restrict our analysis to $K \in \{1, 10, 50\}$.

We calculated the probability mass coverage, as defined in Section \ref{sec:metrics}, for each $K$ value and student-teacher combination. Mass coverage increased with $K$ and reached 0.99 coverage with $K=50$. This increase suggested that increasing $K$ might improve student performance. However, Figure \ref{fig: top-k_effect} contradicts that hypothesis and shows that AIME scores decrease or remain stable as we increase $K$. The only exception is the 0.6B student trained with the 4B teacher, where performance improves as we scale $K$.

This behavior presents another symptom of the capacity gap and shows how a small student struggles to use feedback from a much larger teacher. In all cases, performance either stays the same or decreases when using the 30B and 235B teachers. This behavior shows that larger probability coverage does not necessarily translate into higher student performance. Additionally, it highlights the challenge of giving effective feedback in scenarios with a large capacity gap.

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/best_aime_vs_top_k.png}
    \caption{Effect of increasing the top-$K$ log probabilities. The 0.6B student benefits from increasing $K$ for the smallest teacher, but its performance degrades when using more log probabilities from the 235B teacher. These results suggest that the 0.6B student cannot capture the increasing complexity of the large teacher but benefits from more detailed feedback from a teacher within its reach. For the 4B student, scaling $K$ does not have a positive impact since its performance remains constant or decreases for all teachers.}
    \label{fig: top-k_effect}
\end{figure}
