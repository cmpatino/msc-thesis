\chapter{Experiments}

\section{OPD with Privileged Context} \label{sec: exp_opd_and_pc}

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/opd_pc_aime.png}
    \caption{AIME scores comparing an off-policy distillation method (SFT) with OPD distillation methods. The plots clearly show that using OPD results in better performance for both students regardless the teacher size. Furthermore, performance degrades more for SFT compared to OPD + PC when the teacher size increases, but this effect is seen more clearly in Figure~\ref{fig: aime_gap_scaling}.}
    \label{fig: aime_score_scaling}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/opd_pc_gap_recovery.png}
    \caption{Figure showing the gap recovery rate for all student-teacher combinations. The performance of the 0.6B degrades as we increase the teacher size and the decline is more pronounced for SFT followed by OPD with privileged context. The privileged context also causes the steepest decline to mitigate for the 4B student but still has better performance than SFT.}
    \label{fig: aime_gap_scaling}
\end{figure}


\begin{figure}
    \centering
    \begin{minipage}[t]{0.48\textwidth}
\begin{tcblisting}{
    enhanced,
    listing only,
    title={Student Scoring Context},
    fonttitle=\bfseries,
    coltitle=white,
    colback=blue!2,
    colframe=blue!55!black,
    boxrule=0.6pt,
    arc=1.5mm,
    left=2mm,
    right=2mm,
    top=1.5mm,
    bottom=1.5mm,
    equal height group=scoringctx,
    listing options={
        basicstyle=\ttfamily\scriptsize,
        breaklines=true,
        columns=fullflexible,
        keepspaces=true,
        showstringspaces=false,
        escapeinside={(*@}{@*)}
    }
}
<|im_start|>user
Evaluate the limit: $$\lim_{x\to 0}x \left[ \frac{a}{x}\right]$$ for $a>0$.
<|im_end|>
<|im_start|>assistant
<think>
</think>
We are given the limit:
$$ \lim_{x \to 0^+} x \left[ \frac{a}{x} \right] $$
for $ a > 0 $.
---
### Step 1: Simplify the expression ...
\end{tcblisting}
\end{minipage}%
\hfill
\begin{minipage}[t]{0.48\textwidth}
\begin{tcblisting}{
    enhanced,
    listing only,
    title={Teacher Scoring Context},
    fonttitle=\bfseries,
    coltitle=white,
    colback=green!2,
    colframe=green!45!black,
    boxrule=0.6pt,
    arc=1.5mm,
    left=2mm,
    right=2mm,
    top=1.5mm,
    bottom=1.5mm,
    equal height group=scoringctx,
    listing options={
        basicstyle=\ttfamily\scriptsize,
        breaklines=true,
        columns=fullflexible,
        keepspaces=true,
        showstringspaces=false,
        escapeinside={(*@}{@*)}
    }
}
<|im_start|>user
Evaluate the limit: $$\lim_{x\to 0}x \left[ \frac{a}{x}\right]$$ for $a>0$.

(*@{\bfseries\color{blue!70!black} Your logits are being used to train a 4B model}@*)

<|im_end|>
<|im_start|>assistant
<think>
</think>
We are given the limit:
$$ \lim_{x \to 0^+} x \left[ \frac{a}{x} \right] $$
for $ a > 0 $.
---
### Step 1: Simplify the expression ...
\end{tcblisting}
\end{minipage}
    \caption{Example showing the difference between $\pi_\theta(\cdot \mid x, y_{<n})$ (Student Scoring Context) and $\pi_T(\cdot \mid x, c, y_{<n})$ (Teacher Scoring Context). The privileged context is \emph{Your logits are being used to train a 4B model} we add to the user turn the teacher sees when generating the logits, and it is the only difference between the text scored by the student and teacher policies.}
    \label{fig: pc_example}
\end{figure}

\FloatBarrier

\section{Context Distillation vs. Direct System Prompt} \label{sec: exps_system_prompt}

The simple PC informing the teacher model that its logits were being used to train a small student can noticeable but small effects. We wanted to know if we could use a richer PC that addressed the main failure modes of the results shown in Figures~\ref{fig: aime_score_scaling} and \ref{fig: aime_gap_scaling}.

After an error analysis of the students from Section~\ref{sec: naive_pc}, we classified the errors into 5 categories:

\begin{itemize}
    \item \textbf{Truncation:} Generation cut off by the \texttt{max\_tokens} budget before the model produced an \texttt{ANSWER:} line.
    \item \textbf{Out of range:} Extracted a clean integer outside $[0, 999]$. AIME answers are always in this range, so this is almost certainly a setup or sign error. 
    \item \textbf{Format fail:} Not truncated, but the AIME scorer could not extract a short integer from the response.
    \item \textbf{Off-by-small:} Extracted a clean integer within $±5$ of the target, so this error is typical of an arithmetic slip late in an otherwise correct setup.
    \item \textbf{Wrong Substantive:} Extracted a clean integer within $[0, 999]$ that is more than 5 away from the target. This indicates a substantive error in the reasoning process.
\end{itemize}

As shown by Table~\ref{tab:aime-error-breakdown}, the predominant errors are truncation and wrong substantive. The truncation issue is especially prevalent in the 0.6B student when scaling the teacher. This is a symptom of the student receiving feedback from a model with more complex reasoning patterns but failing to use those reasoning patterns to reach a solution.

To address the two main failure modes, we included a privileged context that encouraged the student to be succinct and write a plan before doing any calculations that may derail its reasoning. For the succinct guideline, we use a portion of the prompt developed by \cite{sang2026CRISPCompressedReasoning}. We exclude the section that refers to the format expected by AIME to focus on the general math reasoning skills instead of on the formatting issues specific of a benchmark.

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/pc_vs_system_gap_recovery.png}
    \caption{Plot comparing the performance of a privileged context with passing the same information through the system prompt to encourage brevity in the model response. The plots show that using the system prompt instead of the privileged information is more effective in terms of AIME performance.}
    \label{fig: pc_vs_system_prompt}
\end{figure}

\red{Talk about AIME scores when ready}

Besides the AIME scores, we also tracked statistic of the completions length through training and show them in Figure XX. The plot shows how the mean length of the completion goes from around 3500 tokens without the targeted PC to around 2000 tokens with the targeted PC intervention. More importantly, the truncated percentage of rollouts decreases from 70\% without PC to 27\% with PC. This decrease in the average length of the rollouts makes the student model even more efficient as it generates answers in fewer tokens.

\begin{figure}
    \centering
    \begin{minipage}[t]{0.48\textwidth}
\begin{tcblisting}{
    enhanced,
    listing only,
    title={Student Scoring Context},
    fonttitle=\bfseries,
    coltitle=white,
    colback=blue!2,
    colframe=blue!55!black,
    boxrule=0.6pt,
    arc=1.5mm,
    left=2mm,
    right=2mm,
    top=1.5mm,
    bottom=1.5mm,
    equal height group=scoringctx,
    listing options={
        basicstyle=\ttfamily\scriptsize,
        breaklines=true,
        columns=fullflexible,
        keepspaces=true,
        showstringspaces=false,
        escapeinside={(*@}{@*)}
    }
}
<|im_start|>user
Evaluate the limit: $$\lim_{x\to 0}x \left[ \frac{a}{x}\right]$$ for $a>0$.
<|im_end|>
<|im_start|>assistant
<think>
</think>
We are given the limit:
$$ \lim_{x \to 0^+} x \left[ \frac{a}{x} \right] $$
for $ a > 0 $.
---
### Step 1: Simplify the expression ...
\end{tcblisting}
\end{minipage}%
\hfill
\begin{minipage}[t]{0.48\textwidth}
\begin{tcblisting}{
    enhanced,
    listing only,
    title={Teacher Scoring Context},
    fonttitle=\bfseries,
    coltitle=white,
    colback=green!2,
    colframe=green!45!black,
    boxrule=0.6pt,
    arc=1.5mm,
    left=2mm,
    right=2mm,
    top=1.5mm,
    bottom=1.5mm,
    equal height group=scoringctx,
    listing options={
        basicstyle=\ttfamily\scriptsize,
        breaklines=true,
        columns=fullflexible,
        keepspaces=true,
        showstringspaces=false,
        escapeinside={(*@}{@*)}
    }
}
<|im_start|>user
Evaluate the limit: $$\lim_{x\to 0}x \left[ \frac{a}{x}\right]$$ for $a>0$.

(*@\begin{minipage}[t]{0.95\linewidth}
\ttfamily\scriptsize\bfseries\color{blue!70!black}
Your logits are being used to train a 4B model.

Follow these guidelines for your answer:

\begin{itemize}
    \setlength{\itemsep}{0.15em}
    \setlength{\topsep}{0.15em}
    \setlength{\parsep}{0pt}
    \item Solve the following math problem concisely and correctly. Be direct; avoid unnecessary elaboration, redundant steps, or restating the problem. Focus only on the key reasoning steps needed to reach the answer.
    \item Before doing any algebra, write a 3--5 line plan: identify the problem type, pick one method, and commit. Do not enumerate multiple approaches in parallel.
\end{itemize}
\end{minipage}@*)

<|im_end|>
<|im_start|>assistant
<think>
</think>
We are given the limit:
$$ \lim_{x \to 0^+} x \left[ \frac{a}{x} \right] $$
for $ a > 0 $.
---
### Step 1: Simplify the expression ...
\end{tcblisting}
\end{minipage}
    \caption{Example showing the privileged context targeted to mitigate the issues with long rollouts.}
    \label{fig: targeted_pc_example}
\end{figure}

\begin{table}[ht]
  \centering
  \caption{Share of incorrect attempts (\% of incorrect) per error category in AIME25.}
  \label{tab:aime-error-breakdown}
  \begin{tabular}{l r r r r r}
    \toprule
    Student / Teacher & Truncation & Wrong substantive & Off-by-small & Out of range & Format fail \\
    \midrule
    4B / 30B       & \textbf{44\%} & \textbf{49\%} & 4\%  & 1\%  & 2\% \\
    4B / 235B     & \textbf{54\%} & \textbf{42\%} & 3\%  & 1\%  & 0\% \\
    0.6B / 4B          & 41\%          & \textbf{46\%} & 3\%  & 7\%  & 3\% \\
    0.6B / 30B     & \textbf{87\%} & 9\%           & 2\%  & 1\%  & 0\% \\
    0.6B / 235B   & \textbf{86\%} & 11\%          & 1\%  & 2\%  & 0\% \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/training_metrics_truncation.png}
    \caption{\red{Actual data but I will probably create a better plot when I have all the trained models with this technique}}
    \label{fig: rollout_length_stats}
\end{figure}


\begin{figure}
    \centering
    \begin{minipage}[t]{0.9\textwidth}
\begin{tcblisting}{
    enhanced,
    listing only,
    title={Student Scoring Context},
    fonttitle=\bfseries,
    coltitle=white,
    colback=blue!2,
    colframe=blue!55!black,
    boxrule=0.6pt,
    arc=1.5mm,
    left=2mm,
    right=2mm,
    top=1.5mm,
    bottom=1.5mm,
    listing options={
        basicstyle=\ttfamily\scriptsize,
        breaklines=true,
        columns=fullflexible,
        keepspaces=true,
        showstringspaces=false,
        escapeinside={(*@}{@*)}
    }
}
<|im_start|>system
(*@\begin{minipage}[t]{0.95\linewidth}
\ttfamily\scriptsize\bfseries\color{blue!70!black}
Your logits are being used to train a 4B model.

Follow these guidelines for your answer:

\begin{itemize}
    \setlength{\itemsep}{0.15em}
    \setlength{\topsep}{0.15em}
    \setlength{\parsep}{0pt}
    \item Solve the following math problem concisely and correctly. Be direct; avoid unnecessary elaboration, redundant steps, or restating the problem. Focus only on the key reasoning steps needed to reach the answer.
    \item Before doing any algebra, write a 3--5 line plan: identify the problem type, pick one method, and commit. Do not enumerate multiple approaches in parallel.
\end{itemize}
\end{minipage}@*)
<|im_end|>
<|im_start|>user
Evaluate the limit: $$\lim_{x\to 0}x \left[ \frac{a}{x}\right]$$ for $a>0$.
<|im_end|>
<|im_start|>assistant
<think>
</think>
We are given the limit:
$$ \lim_{x \to 0^+} x \left[ \frac{a}{x} \right] $$
for $ a > 0 $.
---
### Step 1: Simplify the expression ...
\end{tcblisting}
\end{minipage}
    \caption{Example showing the privileged context targeted to mitigate the issues with long rollouts.}
    \label{fig: system_prompt_example}
\end{figure}

\FloatBarrier

\section{OPD vs Off-Policy Methods} \label{sec: exps_off_vs_on}

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/off_vs_on_policy.png}
    \caption{Effect of increasing the top-k log probabilities. The 0.6B benefits from increasing K for the smallest teacher, but its performance degrades when using more logprobs from the 235B teacher. These results suggest that the 0.6B student cannot capture the increasing complexity of the large teacher but benefits from more details from a teacher within its reach. For the 4B student, scaling K does not have a positive impact since its performance remains constant or decreases for all teachers.}
    \label{fig: opd_vs_off-policy}
\end{figure}



\section{Extending from top-1 to top-k Log-Probabilities} \label{sec: exps_top_k}

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/best_aime_vs_top_k.png}
    \caption{Effect of increasing the top-k log probabilities. The 0.6B benefits from increasing K for the smallest teacher, but its performance degrades when using more logprobs from the 235B teacher. These results suggest that the 0.6B student cannot capture the increasing complexity of the large teacher but benefits from more details from a teacher within its reach. For the 4B student, scaling K does not have a positive impact since its performance remains constant or decreases for all teachers.}
    \label{fig: top-k_effect}
\end{figure}

\FloatBarrier

\section{Catastrophic Forgetting} \label{sec: exps_forgetting}

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/teacher_scaling_gpqa_top1.png}
    \caption{Scores for GPQA for all students while training targeting math reasoning skills. The plots show how the 0.6B student suffers from catastrophic forgetting for the three methods, but also how SFT is the method that degrades performance the most. For the 4B student, all methods preserve the GPQA performance and even marginally improve it. These results show how the main driver in catastrophic forgetting is model capacity, and that on-policy methods are better to mitigate performance degradation.}
    \label{fig: gpqa_forgetting}
\end{figure}

\FloatBarrier
