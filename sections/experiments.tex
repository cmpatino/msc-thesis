\chapter{Experiments}

\section{Scaling the Teacher}

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/teacher_scaling_aime_top1.png}
    \caption{Caption}
    \label{fig: aime_score_scaling}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/teacher_scaling_gap_recovery.png}
    \caption{Caption}
    \label{fig: aime_gap_scaling}
\end{figure}

\section{Extending from top-1 to top-k Log-Probabilities}

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/teacher_scaling_gap_recovery_top_k.png}
    \caption{Caption}
    \label{fig: top-k_gap_scaling}
\end{figure}



\section{Sample Efficiency}

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/learning_curves_aime_qwen3_0.6b.png}
    \caption{Caption}
    \label{fig: pc_0.6B_learning_curve}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{figures/learning_curves_aime_qwen3_4b.png}
    \caption{Caption}
    \label{fig: pc_4B_learning_curve}
\end{figure}

\section{Using TAID with On-Policy Rollouts and RKL}

\section{The capacity gap appears clearly under naive off-policy distillation}

SFT on teacher completions performs poorly, especially for the 0.6B student, and larger teachers make things worse. This establishes the problem.

\section{On-policy distillation is a better teacher-student interface}

OPD substantially improves AIME over SFT for both students and preserves IFEval. This is the main positive result.

\section{Hard top-1 OPD is still information-limited}

Top-1 labels compress the teacher distribution too aggressively. Diagnostics show high top-1 agreement between 30B and 235B teachers, so the larger teacher’s advantage is partly hidden.


\section{Distributional top-K objectives partially address that bottleneck}

Bucketed RKL exposes more of the teacher distribution and works best for the 4B student with the 235B teacher. But K=10 is not enough to make the 235B teacher useful for the 0.6B student.

\section{Using an intermediate teacher is useful to address the gap. (?)}

\section{Privileged context is useful but not a general solution}

\begin{tcolorbox}[
    enhanced,
    breakable,
    colback=gray!3,
    colframe=black!60,
    coltitle=white,
    title={Teacher and student contexts for log-probability scoring},
    fonttitle=\bfseries,
    boxrule=0.6pt,
    arc=1.5mm,
    left=2mm,
    right=2mm,
    top=1.5mm,
    bottom=1.5mm
]
For privileged-context distillation, the same completion is scored by both
models, but the conditioning context differs. The teacher is evaluated as
$\pi_T(\cdot \mid x, c, y_{<n})$: the user turn contains the original prompt
plus the privileged context (PC), and the assistant turn contains the
completion prefix. The student is evaluated as
$\pi_\theta(\cdot \mid x, y_{<n})$: it sees the original prompt and the same
completion prefix, but never the PC.
\end{tcolorbox}

\begin{tcblisting}{
    enhanced,
    breakable,
    listing only,
    title={Teacher scoring context},
    fonttitle=\bfseries,
    coltitle=white,
    colback=blue!2,
    colframe=blue!55!black,
    boxrule=0.6pt,
    arc=1.5mm,
    left=2mm,
    right=2mm,
    top=1.5mm,
    bottom=1.5mm,
    listing options={
        basicstyle=\ttfamily\scriptsize,
        breaklines=true,
        columns=fullflexible,
        keepspaces=true,
        showstringspaces=false
    }
}
<|im_start|>user
Evaluate the limit: $$\lim_{x\to 0}x \left[ \frac{a}{x}\right]$$ for $a>0$.

Privileged context: Your logits are being used to train a 4B model
<|im_end|>
<|im_start|>assistant
<think>
</think>
We are given the limit:
$$ \lim_{x \to 0^+} x \left[ \frac{a}{x} \right] $$
for $ a > 0 $.
---
### Step 1: Simplify the expression ...
\end{tcblisting}

\begin{tcblisting}{
    enhanced,
    breakable,
    listing only,
    title={Student scoring context},
    fonttitle=\bfseries,
    coltitle=white,
    colback=green!2,
    colframe=green!45!black,
    boxrule=0.6pt,
    arc=1.5mm,
    left=2mm,
    right=2mm,
    top=1.5mm,
    bottom=1.5mm,
    listing options={
        basicstyle=\ttfamily\scriptsize,
        breaklines=true,
        columns=fullflexible,
        keepspaces=true,
        showstringspaces=false
    }
}
<|im_start|>user
Evaluate the limit: $$\lim_{x\to 0}x \left[ \frac{a}{x}\right]$$ for $a>0$.
<|im_end|>
<|im_start|>assistant
<think>
</think>
We are given the limit:
$$ \lim_{x \to 0^+} x \left[ \frac{a}{x} \right] $$
for $ a > 0 $.
---
### Step 1: Simplify the expression ...
\end{tcblisting}

PC can improve top-1 OPD for the tiny student and can preserve GPQA in some settings, but PC+RKL does not currently improve the main AIME results. Therefore PC should be framed as a conditional teacher-side aid, not the core solution.

\section{Different PC is not useful to improve performance}
